package model.ifaces.actor.shape.rectangle.image;

import model.ifaces.actor.shape.rectangle.RectangleListener;

public interface ImageListener extends RectangleListener {

	public void iconChanged(ImageModel model);
}
